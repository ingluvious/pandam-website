import React from "react";

const Services: React.FC = () => {
    return(
        <div style = {{padding: '20px'}}>
            <h1> Hello! </h1>
            <p> We do the impossible, so that it's possible!</p>
        </div>
    );
};

export default Services